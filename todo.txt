1a. add moderateMessage to server settings 
1b. add a whitelisted channel array to server settings
    1b.. add a whitelist flag array to server settings
1c. mute user if their messages get flagged 3 times in 1 minute

2. create a helper method to see if modlog channel is present
2. add a gateway disconnect listener and in that event, pause or scheduled tasks in database or cache
3. purge until (message id) command
4. organize mute.java code better to be more clear