1. make settings modal handler respond with a test join message
2. make settings modal handler respond with an embed showing all the settings displayed nicely
3. update commands and listeners so they are redundant when database is unavailable.
    for example make it so you can mute members still by choosing the role from a selectmenu